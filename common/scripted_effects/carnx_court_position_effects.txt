# VANILLA COURT POSITIONS

# court_physician_court_position - Yes*
# cultural_emissary_court_position - Yes
# keeper_of_swans_court_position - No [country-specific (England)] [vassal]
# chief_qadi_court_position - No [religious]
# garuda_court_position - No* [culture-specific (Kannada)] [multiple]
# court_gardener_court_position - Yes* [culture-specific (Persian)]
# lady_in_waiting_court_position - No* [multiple]
# antiquarian_court_position - Yes*
# court_tutor_court_position - Yes*
# food_taster_court_position - Yes*
# master_of_horse_court_position - No [vassal]
# master_of_hunt_court_position - No [vassal]
# royal_architect_court_position - No [vassal]
# high_almoner_court_position - No* [religious]
# seneschal_court_position - Yes*
# cupbearer_court_position - Yes*
# chief_eunuch_court_position - Yes* [culture-specific (Han, Hindustani, Tangut, Greek)]
# court_jester_court_position - Yes*
# court_poet_court_position - Yes*
# court_musician_court_position - Yes*
# bodyguard_court_position - Yes* [multiple]
# champion_court_position - Yes*
# executioner_court_position - Yes*
# court_artificer_court_position - Yes [multiple]

# CarnalitasX Court Positions

# carnx_slave_concubine_court_position
# carnx_slave_entertainer_court_position
# carnx_slave_eunuch_court_position
# carnx_mameluke_captain_court_position

# COURT PHYSICIAN

court_physician_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Physician court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

court_physician_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Physician court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

court_physician_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Physician court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# ANTIQUARIAN

antiquarian_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Antiquarian court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

antiquarian_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Antiquarian court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

antiquarian_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Antiquarian court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# COURT EUNUCH

chief_eunuch_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Chief Eunuch court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

chief_eunuch_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Chief Eunuch court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

chief_eunuch_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Chief Eunuch court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# COURT JESTER

court_jester_title_accepted_effect = {
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Jester court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

court_jester_title_revoked_effect = {
	scope:employee = {
		if = { #Only lowborn/unlanded are insulted if it's revoked
			limit = {
				NOR = {
					is_lowborn = no
					is_landed = yes
				}
			}
			add_opinion = {
				modifier = insult_opinion
				target = liege
				opinion = 15
			}
		}
	}
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Jester court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

court_jester_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Jester court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# HIGH ALMONER

high_almoner_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "High Almoner court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

high_almoner_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "High Almoner court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

high_almoner_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "High Almoner court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# CUPBEARER

cupbearer_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Cupbearer court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

cupbearer_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Cupbearer court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

cupbearer_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Cupbearer court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# SENESCHAL

seneschal_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Seneschal court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

seneschal_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Seneschal court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

seneschal_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Seneschal court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# COURT TUTOR

court_tutor_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Tutor court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

court_tutor_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Tutor court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

court_tutor_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Tutor court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# COURT POET

court_poet_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Poet court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

court_poet_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Poet court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

court_poet_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Poet court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
s}

# BODYGUARD

bodyguard_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Bodyguard court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

bodyguard_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Bodyguard court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

bodyguard_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Bodyguard court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# CHAMPION

champion_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Champion court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

champion_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Champion court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

champion_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Champion court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# COURT MUSICIAN

court_musician_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Musician court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

court_musician_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Musician court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

court_musician_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Musician court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# FOOD TASTER

food_taster_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Food Taster court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

food_taster_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Food Taster court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

food_taster_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Food Taster court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# LADY-IN-WAITING

lady_in_waiting_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Lady-in-waiting court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

lady_in_waiting_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Lady-in-waiting court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

lady_in_waiting_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Lady-in-waiting court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# EXECUTIONER

executioner_title_accepted_effect = {
	scope:liege = {
		consume_imprisonment_reasons = scope:employee
	}
	scope:employee = {
		if = {
			limit = { is_imprisoned = yes }
			release_from_prison = yes
		}
	}
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Executioner court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

executioner_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Executioner court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

executioner_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Executioner court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# GARUDA

garuda_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Garuda court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

garuda_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Garuda court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

garuda_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Garuda court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# COURT GARDENER

court_gardener_title_accepted_effect = {
	basic_gained_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Garedner court position received"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

court_gardener_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Garedner court position revoked"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

court_gardener_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	if = {
		limit = {
			scope:employee = { has_trait = slave }
		}
		debug_log = "Court Garedner court position invalidated"
		debug_log_date = yes
		debug_log_scopes = yes
	}
}

# SLAVE CONCUBINE

carnx_slave_concubine_title_received_effect = {
	basic_gained_court_position_effect = yes
	debug_log = "Slave Concubine court position received"
	debug_log_date = yes
	debug_log_scopes = yes
}

carnx_court_physician_title_revoked_effect = {
	basic_revoked_court_position_effect = yes
	debug_log = "Slave Concubine court position revoked"
	debug_log_date = yes
	debug_log_scopes = yes
}

carnx_court_physician_title_invalidated_effect = {
	basic_invalidated_court_position_effect = yes
	debug_log = "Slave Concubine court position invalidated"
	debug_log_date = yes
	debug_log_scopes = yes
}