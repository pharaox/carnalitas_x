# Dummy travel effect, overridden by Travelers
travl_travel_effect = {
	if = {
		limit = {
			exists = $SENDER$
			exists = $RECIPIENT$
			exists = $ORIGIN$
			list_size = { name = $COMPANIONS$ value >= 0 }
		}

		carnx_debug_log_effect = { MSG = carnx_debug_msg_travel_requested }
	}
}
